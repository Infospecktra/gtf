/* Generated by Together */
package org.yang.customized.gtf.services.dataAccess;

import org.yang.web.view.controls.WebControl;
import javax.servlet.jsp.JspWriter;
import org.yang.web.view.controls.jsStyle.dataSheet.TextItem;
import org.yang.util.SMUtility;

/**
 * @testcase org.test.org.yang.web.view.controls.jsStyle.TestListItem 
 */
public class DateTextItem extends TextItem
{
   private int date = 0;

   public DateTextItem()
   {
      super();
   }

   public void setText(String text)
   {
      super.setText(text);
      try
      {
         String[] dateArray = SMUtility.splitByToken(texts[0], "-/", false);
         if(null!=dateArray&&3==dateArray.length)
         {
            date = 10000*Integer.parseInt(dateArray[2]) +
                   100*Integer.parseInt(dateArray[0])+
                   Integer.parseInt(dateArray[1]);
         }
      }
      catch(Exception e)
      {
         System.out.println("[DateTextItem::setText] Unable to parse date string : " + texts[0]);
      }
   }

   public int compareTo(Object object)
   {
      try
      {
         int temp = (((DateTextItem)object).date);
         if(date>temp)
            return 1;
         else if(date==temp)
            return 0;
         else
            return -1;
      }
      catch(Exception e)
      {
         e.printStackTrace();
         return 0;
      }
   }
}
package org.yang.util.template;

import java.io.*;
import org.w3c.dom.*;

/**
 * @testcase com.test.com.IMDSTrading.marketDecisionEngine.util.TestDomConverter 
 */
public interface DomConverter
{  
   public Document convert(String ct);

   public Document convert(byte[] ct);

   public Document convert(InputStream ct);
}
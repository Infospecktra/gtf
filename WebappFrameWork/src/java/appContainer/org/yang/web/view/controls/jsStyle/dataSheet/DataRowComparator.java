/* Generated by Together */

package org.yang.web.view.controls.jsStyle.dataSheet;
import java.util.Comparator;

public class DataRowComparator implements Comparator
{
   protected String type = null;
   protected boolean isAscending = true;

   /**
    * Call the constructor and set the id which represents this content.
    *
    * @param      id     the String indicates identification of this content.
    */
   //
   public DataRowComparator(String type, boolean isAscending)
   {
      //&System.out.println("[DataRowComparator::DataRowComparator] Prepare Sort by:" + type + ", isAscending:" + isAscending);
      this.type = type;
      this.isAscending = isAscending;
   }
   //

   /**
    * Call the constructor and set the id which represents this content.
    *
    * @param      id     the String indicates identification of this content.
    */
   //
   public DataRowComparator(String type)
   {
      this(type, true);
   }
   //
   
   /**
    * Call the constructor and set the id which represents this content.
    *
    * @param      id     the String indicates identification of this content.
    */
   //
   public int compare(Object data1, Object data2)
   {
      if(null==data1||null==data2)
         throw new NullPointerException();

      DataRow row1 = (DataRow)data1;
      DataRow row2 = (DataRow)data2;

      if(row1.getOrder()>row2.getOrder())
         return 1;
      else if(row2.getOrder()>row1.getOrder())
         return -1;
      else
      {
         DataItem item1 = row1.getItem(type);
         DataItem item2 = row2.getItem(type);

         if(null==item1||null==item2)
            return 0;

         return isAscending?item1.compareTo(item2):item2.compareTo(item1);
      }
   }

   /**
    * Call the constructor and set the id which represents this content.
    *
    * @param      id     the String indicates identification of this content.
    */
   //
   public boolean equals(Object comparator)
   {
      return false;
   }
   //
}
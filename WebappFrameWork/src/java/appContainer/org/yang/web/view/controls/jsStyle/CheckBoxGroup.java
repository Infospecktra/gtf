/* Generated by Together */

package org.yang.web.view.controls.jsStyle;
import javax.servlet.jsp.JspWriter;
import java.util.HashSet;
import org.yang.web.view.controls.WebControl;
import java.util.ArrayList;

public class CheckBoxGroup extends FormElement 
{
   private String id = "";
   public void setId(String id) { this.id = id; }

   private String name = null;
   public void setName(String name) { this.name = name; }
   public String getName() { return name; }

   private String onClick = null;
   public void setOnClick(String onClick) { this.onClick = onClick; }

   public String[] getValues()
   {
      ArrayList temp = new ArrayList();
      int size = super.mySubcontrols.size();
      CheckBox control = null;
      for(int i=0; i<size; i++)
      {
          control = (CheckBox)mySubcontrols.get(i);
          if(control.getIsChecked())
             temp.add(control.getValue());
      }
      return (String[])temp.toArray(new String[]{});
   }

   public void printBody(JspWriter writer) throws Exception
   {
      int size = super.mySubcontrols.size();
      CheckBox control = null;

      for(int i=0; i<size; i++)
      {
         try
         {
            control = (CheckBox)super.mySubcontrols.get(i);
            control.setName(name);
            control.setOnClick(this.onClick);
         } catch(Exception e) { continue; }
         control.printBody(writer);
      }
   }
}
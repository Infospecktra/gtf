/* Generated by Together */

package org.yang.web.view.controls.jsStyle;

import javax.servlet.jsp.JspWriter;
import org.yang.web.view.controls.WebControl;

public class OperationElement extends WebControl
{
   private String name = null;
   public void setName(String name) { this.name = name; }

   private String areaname = null;
   public void setAreaname(String areaname) { this.areaname = areaname; }

   private String checkedIcon = null;
   public void setCheckedIcon(String checkedIcon) { this.checkedIcon = checkedIcon; }

   private String uncheckedIcon = null;
   public void setUncheckedIcon(String uncheckedIcon) { this.uncheckedIcon = uncheckedIcon; }

   private boolean isChecked = false;
   public void setIsChecked(boolean isChecked) { this.isChecked = isChecked; }

   private boolean isReadOnly = true;
   public void setIsReadOnly(boolean isReadOnly) { this.isReadOnly = isReadOnly; }

   public void render(JspWriter writer) throws Exception
   {
      if(isChecked)
      {
         if(isReadOnly)
            writer.println("          <img src=\"" + checkedIcon + "\" align=\"absmiddle\">" + name);
         else
	        writer.println("          <input type=\"checkbox\" name=\"" + areaname + "\" value=\"" + name + "\" checked >" + name);
      }
      else
      {
         if(isReadOnly)
            writer.println("          <img src=\"" + uncheckedIcon + "\" align=\"absmiddle\">" + name);
         else
	        writer.println("          <input type=\"checkbox\" name=\"" + areaname + "\" value=\"" + name + "\" >" + name);
      }
   }
}
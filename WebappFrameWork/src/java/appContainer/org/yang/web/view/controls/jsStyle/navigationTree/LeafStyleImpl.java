/* Generated by Together */

package org.yang.web.view.controls.jsStyle.navigationTree;

import javax.servlet.jsp.JspWriter;
import java.io.IOException;
public class LeafStyleImpl extends NavigationNode
{
   private static String LEAF_END = "/images/node.gif";
   private static String LAST_LEAF_END = "/images/lastnode.gif";

   private String vertlineEnd = null;

   protected String getStyle()
   {
      return "LEAFSTYL";
   }

   protected void printMyIcon(JspWriter writer) throws IOException
   {
      String icon = null;
      if(this.isExpanded)
         icon = this.iconOpen;
      else
         icon = this.iconClose;

      writer.print("<img src=\"" + icon + "\"" );
      writer.print(" alt=\"ADMIN\"" );
      writer.print(" border=\"0\">" );
   }

   protected void printSpacer(JspWriter writer) throws IOException
   {
      writer.print("</td>" );
      writer.print("<td nowrap>" );
   }

   protected void printVertlineEnd(JspWriter writer) throws IOException
   {
      if(isTheLastNode)
         vertlineEnd = LAST_LEAF_END;
      else
         vertlineEnd = LEAF_END;

      writer.print("<img src=\"" + codeBase + vertlineEnd + "\"" );
      writer.print(" width=\"16\"" );
      writer.print(" height=\"22\">" );
   }
}